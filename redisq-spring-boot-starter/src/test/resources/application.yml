#
#
#
#
#
redisq:
  consumer:
    - topic: topic1
      consume-timeout: 1000
      consume-threads: 3
      retry:
        type: multi
        max: 3
      processor: zhyyy
  producer:
    - topic: topic1
      ttlSeconds: 60
      serializer: org.chobit.spring.redisq.beetle.serialization.JacksonSerializer

